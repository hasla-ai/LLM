# 기준선 검토 결과 — 2026-09-22

문서 ID: FHZ-FINDING-001
검토 범위: `docs/03-governance/`, `workflow/`, `schemas/`, `app/`, `knowledge/`
상태: F-01 해소(D-024) · F-02 범위 정정(D-025)·선행관계 확정(D-028) · F-03 해소(D-026) · F-05 해소(D-027) · F-08 해소(D-029) — F-02·F-07 미해소

---

## F-01 (중대·해소됨) 단계 분류가 세 파일에서 서로 다르다

미션 20개가 잘못된 전문가 Agent 에게 라우팅된다.

### 관측

`docs/03-governance/mission-catalog.md` 의 단계 구분:

| 미션 | 카탈로그 단계 |
|---|---|
| M-021 ~ M-030 | 입지·토지·GIS |
| M-031 ~ M-040 | 수소 생산·공급·저장 |
| M-041 ~ M-050 | 발전기·열·전력계통 |
| M-051 ~ M-060 | 개념설계 |

`workflow/process-agent-map.json` 의 단계 구분:

| phase | 이름 | 미션 |
|---|---|---|
| P03 | site | M-021 ~ M-040 |
| P04 | hydrogen | M-041 ~ M-050 |
| P05 | power_grid | M-051 ~ M-060 |

즉 `M-041 select_power_technology` 는 카탈로그상 발전 미션인데 workflow 맵에서는
**수소 단계**의 리드 Agent 에게 배정된다. `M-051 freeze_design_basis` 는 개념설계
미션인데 **계통 단계**로 간다. M-041 ~ M-060 의 20개가 전부 한 칸씩 밀려 있다.

여기에 `schemas/mission-definition.schema.json` 의 `phase` enum 은

```
intake | business | site | hydrogen | power_grid | concept_design |
safety_permit | feed_epc | construction | operations
```

로 `concept_design` 을 갖고 `commissioning` 이 없다. workflow 맵은 정반대로
`commissioning` 이 있고 `concept_design` 이 없다. 세 파일이 전부 다르다.

### 영향

- Agent 호출·회의 소집·승인 역할이 20개 미션에서 잘못 결정된다.
- `mission-definition.schema.json` 을 만족하는 미션 정의를 쓸 수 없다.
  어떤 phase 값을 넣어도 세 문서 중 둘과 어긋난다.

### 조치 — 완료 (2026-09-23)

`mission-catalog.md` 를 정본으로 채택했다 (D-024). 카탈로그만이 100개 미션 전건의
ID·함수·입력·출력·담당·Gate 를 한 표에 갖고 있어 다른 두 파일을 카탈로그에서 파생할 수
있다. 역방향은 불가능하다.

`schemas/mission-definition.schema.json` 의 phase enum 은 **변경하지 않았다.** 검사 결과
enum 10개가 이미 카탈로그의 10단계와 이름·순서가 일치했다. 어긋나 있던 것은 workflow
맵뿐이었다.

`workflow/process-agent-map.json` 을 v1.1.0 으로 재작성했다.

- 10개 단계 전부를 카탈로그의 10미션 블록에 맞췄다. 실제 드리프트는 이 문서가 처음
  기록한 M-041~M-060 보다 넓었다. P01/P02 경계가 M-006 에서, P03 이 site 와 hydrogen
  20개를 한 단계로 삼키는 지점에서도 어긋나 있었다.
- 카탈로그에 없는 `commissioning` 단계를 제거하고, 리드 Agent·필수 Agent·산출물·차단조건을
  `construction` 으로 흡수했다. 카탈로그 Phase 8 이 M-087~M-089 시운전 미션(G6)을
  포함하기 때문이다.
- `concept_design` 단계(P06, M-051~M-060, G3)를 신설했다.
- 카탈로그는 미션별 Gate 를 주고 Phase 8 은 G5/G6/G7 에 걸친다. 단일 `gate_id` 로
  표현할 수 없어 `gate_ids` 와 `mission_gates` 를 추가했다.
- `international_overlay_phase_controls` 를 이름 기준으로 재매핑했다.

### 회귀 방지

`tools/check_phase_alignment.py` 가 세 파일의 일치를 검사한다. 정본 자체의 무결성(미션
100개 연속·중복 없음·단계별 연속 블록)도 함께 본다.

```
python tools/check_phase_alignment.py
```

수정 전 HEAD 기준 불일치 35건을 검출하고, 수정 후 0건으로 통과한다. 미션 한 칸 밀기·단계
이름 훼손·복수 Gate 정보 삭제·enum 값 교체·정본 미션 삭제 5개 변조를 주입해 전부
탐지되는 것을 확인했다.

---

## F-02 (중대) 미션 카탈로그가 자기 스키마를 만족하지 않는다

`schemas/mission-definition.schema.json` 은 다음을 **필수**로 요구한다.

- `dependencies`
- `completion_criteria`
- `block_conditions`
- `input_contract.required_fields` / `units_required`
- `output_contract.quality_checks`
- `review_policy`

`mission-catalog.md` 의 100행 표에는 이 중 어느 것도 없다. 함수명·입력·출력·담당·Gate
만 있다. 스키마는 정의됐지만 그것을 만족하는 미션 정의가 하나도 없다.

### mission-library-100.json 은 이식 원본이 될 수 없다 (2026-09-23 정정)

이 문서는 당초 `mission-library-100.json` 의 선행관계·완료기준·차단조건을 카탈로그로
이식하면 된다고 적었다. **그 전제가 틀렸다.** ID 체계와 게이트 수 차이는 표면적인
문제였고, 실제로는 두 문서가 서로 다른 100개 미션 분해다.

| 비교 | 결과 |
|---|---|
| 위치 대응(M-0NN ↔ M0NN)에서 산출물 엔티티가 일치 | **2 / 100** |
| 라이브러리 전체에서 대응 미션을 찾을 수 있음 | **3 / 100** |

단계 구성 자체가 다르다. 라이브러리는 단계마다 게이트가 하나씩 붙은 G0~G9 이고
상업운전(COD)에서 끝난다. 카탈로그는 게이트가 단계를 가로지르는 G0~G7 이고 운영·정비
단계를 더 갖는다. 라이브러리 P5 `설계기준·FEED` 는 카탈로그에서 Phase 5 개념설계와
Phase 7 FEED·EPC 로 쪼개지고, 라이브러리 P6 안전성·P7 인허가는 카탈로그 Phase 6 하나로
합쳐진다. 카탈로그 Phase 9(운영)에 대응하는 라이브러리 단계는 없다.

ID 를 맞춰 병합하면 97개 미션에 **틀린 선행관계·완료기준·차단조건이 들어간다.**
매핑 테이블을 만들 대상이 사실상 없다. 라이브러리는 참고 자료로만 남긴다 (D-025).

### 카탈로그만으로 채울 수 있는 범위 (2026-09-23 측정)

`tools/build_mission_definitions.py` 가 정본에서 도출 가능한 것과 아닌 것을 나눈다.

| 필드 | 상태 |
|---|---|
| `mission_id` · `code` · `phase` · `owner_role` · `gate` | 카탈로그에서 직접 도출 |
| `output_contract.required_fields` | 카탈로그 출력 칸에서 도출 |
| `risk_class` · `review_policy` · `executor` | 규칙으로 제안, **확인 필요** |
| `purpose` · `completion_criteria` · `block_conditions` | 카탈로그 기반 내용 초안 작성 완료, 자격자 검토 필요 |
| `quality_checks` · `schema_ref` | 초안 100/100 작성·등록, 자격자 검토 필요 |

`dependencies` 는 산출물→입력 사슬로 도출을 시도했다.

| 입력 토큰 235건 | |
|---|---|
| 정확 일치 | 18 |
| 부분일치 유일 → 선행관계 확정 | 55 |
| 부분일치 모호 → **확정 불가** | 29 |
| 산출 미션 없음 (외부 입력·비엔티티) | 133 |

모호한 29건은 카탈로그의 입력 칸이 약어라서 생겼다. 예를 들어 M-003 의 입력 `Intent` 는
M-001 의 `ProjectIntent` 인지 M-002 의 `NormalizedIntent` 인지 카탈로그만으로 결정되지
않는다.

**29건은 2026-09-23 에 전건 확정했다 (D-028).** 적용 규칙은 결정지
`docs/03-governance/mission-dependency-decisions.json` 에 있다.

- R1 단계교차 — 다른 단계 산출물은 그 단계의 종결·게이트 미션에 건다
- R2 단계내 — 같은 단계 안에서는 해당 엔티티를 실제 산출한 미션에 건다
- R3 단위 — `Intent` 는 정규화 이후(M-002)를 쓴다

결정 직후 결과는 **선행관계 100건(66개 미션), 모호 0건.** 결속 근거는 28건이 실제 산출 미션
(`producer`), 1건이 게이트 선행조건(`gate_precondition`, M-058 의 `Grid`)이며 미션별
`extensions.dependency_derivation.resolved_ambiguities` 에 남겼다.

D-029 적용 후 M-046→M-047 선행관계가 1건 추가되어 현재 생성본은 **101건(66개 미션),
모호 0건, 전방참조 0건**이다.

### 현재 상태

`docs/03-governance/mission-content-draft.json`에 100개 미션의 목적·완료기준·차단조건을
작성했고, `mission-contract-draft.json`에 입력·출력 `schema_ref`와 출력 `quality_checks`를
추가해 생성 결과에 반영했다. `mission-definitions.draft.json`은 100건·스키마 적합
100/100·미션 내용 100/100·전체 TODO 0건이다. `schemas/mission-contracts.schema.json`에
필수 필드 존재 계약 200개를 등록했지만, 값의 도메인 타입·단위·근거 요건과 자격자 검토가
남아 있어 기준선은 아니다. 담당자 검토 큐 100건은 모두 `pending`이고, 고유 입출력 토큰
249건은 `entity-contract-review-queue.json`에서 모두 `pending_owner_review`다.

```
python tools/build_mission_definitions.py   # 정본과 내용 원장에서 초안 생성
python tools/check_mission_definitions.py   # 스키마·미션 내용·정본 정합성 검사
```

검사기는 스키마 적합, 미션 내용, 전체 계약 완결을 **따로 센다.** 입력·출력 계약에
자리표시자(`TODO-AUTHOR`)가 남아 있어도 스키마는 통과할 수 있기 때문이다. 이를 한 숫자로
합치면 초안이 완성본으로 오인된다.

F-02 는 아직 미해소다. 선행관계와 미션 내용·필드 존재 계약 초안은 확정·작성했고
(D-028·D-029·D-030·D-031·D-032·D-033), 도메인 타입·단위·근거 요건의 구체화, 미션별
자격자 검토·Gate 증거 기준 확인이 남아 있다. D-034 보고서에 100개 미션·249개 토큰별 제안과 한계를 기록했고, D-035 2차 회의록에서 IR-01~IR-07 및 DR-01~DR-10의 재현·후속조치를 분리했다. 커밋 `6c529d5`의 도메인 검토 보충자료(미션 13건·단위 후보 33건)도 D-036으로 접수했지만, 모든 결정은 `proposed_not_approved`다. 안전·계통·시운전 입력 공백은 자격자 결정 전 HOLD이며 초안은 기준선으로 승격하지 않는다.

---

## F-03 (중대) `app/main.py` 의 계산이 거버넌스를 따르지 않는다

`/api/mvp/feasibility` 의 결함.

### F-03a 상수가 코드에 박혀 있다

```python
h2_kwh_per_kg = 33.33
```

`knowledge/formula-library.json` 이 존재하고 `requires_unit_check: true` 까지 선언돼
있는데 앱은 라이브러리를 참조하지 않는다. 정확값은 33.322 kWh/kg (119.96 MJ/kg) 이며
수치 오차 자체는 0.02% 로 무의미하다. 문제는 **출처 없는 상수가 코드에 사는 것**이다.

### F-03b 총발전단과 순출력이 구분되지 않는다

`power_mw` 가 어느 쪽인지 정의가 없고 소내부하가 모델에 없다. 순출력을 입력하면
실제 필요 수소량이 소내부하율(통상 5~12%)만큼 과소산정된다. 공급계약 물량과 저장
설비가 이 값에서 나온다.

### F-03c 저장 산정에 사용가능 잔압이 빠져 있다

```python
storage_kg = h2_kg_per_hour * 24 * payload.storage_days
```

압축가스는 최소사용압력 아래를 인출할 수 없다. 설치 저장량은 이 값을 사용가능
비율(통상 0.7~0.85)로 나눈 것이어야 한다. 빠지면 저장설비가 15~30% 과소산정된다.

### F-03d 저장밀도 기본값의 근거가 없다

`storage_density_kg_m3 = 30.0` 은 대략 450~500 bar 에 해당한다. 200 bar / 288 K /
Z=1.10 에서 수소 밀도는 약 **15.3 kg/m³** 다. 밀도가 2배 틀리면 저장 용적이 2배
틀리고, `storage_volume_m3 * 3.0` 을 타고 부지면적까지 틀어진다.

### 조치 — 완료 (2026-09-23, D-026)

`/api/mvp/feasibility` 가 `app/calc.py` 계산 커널을 호출하도록 연결했다. 기존 `derived`
응답 키는 유지해 화면·기존 호출자의 계약을 보존하고, `calculation`에 공식 ID·가정·근거등급을
추가했다. `power_mw`는 총발전단으로 명시하고 소내부하율에서 순출력을 계산한다. 저장 설치량은
사용가능 저장비율을 반영하며, 기본 저장밀도는 압력·온도·압축계수에서 계산한다. 공급사
저장밀도는 명시적 override일 때만 사용한다.

회귀 검증은 `tests/test_calc_integration.py` 3건으로 수행한다. 기존의 33.33 kWh/kg 상수,
사용가능 잔압 누락, 30 kg/m³ 무근거 기본값은 API 경로에서 제거됐다.

---

## F-04 (보통) 공식 라이브러리에 수소 고유식이 없었다

`knowledge/formula-library.json` 의 15개는 옴의 법칙·베르누이·훅의 법칙 같은 일반
공학 기초식이며, 수소발전 플랜트 고유식은 하나도 없었다.

`knowledge/formula-library-h2.json` 을 추가했다. 18식, 검증 케이스 22개, 전부
`machine_checked`. 자격자·발행기관 확인 전이므로 `expert_verified` 로 올리지 않았다.

---

## F-05 (보통) 레포에 테스트가 없다 — 해소됨 (2026-09-23, D-027)

기존 15개 공식 중 **14개에 없던 `validation_cases`를 모두 추가했다.**

`tests/` 회귀 테스트와 `.github/workflows/quality.yml` CI를 추가했다. CI는 Python 3.11·3.12에서
회귀 테스트, 공식·스키마 검증, 단계 정합성, 미션 정의 골격, 보호 감사를 실행한다. 보호 감사는
실패 시 비정상 종료한다. 현재 결과:

```
검증 케이스 통과 37 · 실패 0 · 경고 0
스키마 통과 6 · 실패 0
```

기존 15개 공식과 H2 공식 18개를 합쳐 37건의 검증 케이스가 통과한다. 공식의
`verification_status`는 자격자·발행기관 확인 전이므로 여전히 `unverified`다.

---

## F-06 (경미) 스키마를 검증할 수 있는 도구가 없었다

`schemas/*.schema.json` 은 `$id` 가 `https://forge-h2.local/...` 절대 URL 이고
`$ref` 는 상대 경로다. 로컬 레지스트리 없이는 해석되지 않아 검증 시도 자체가
네트워크 요청으로 빠진다. `tools/validate_formulas.py` 가 로컬 레지스트리를 구성한다.

---

## 우선순위

1. **F-02** — 자격자 작성이 필요한 미션 정의 필드와 29건의 모호한 선행관계

---

## F-08 (중대·해소됨) 정본 카탈로그에 전방참조가 있다

선행관계 DAG 를 도출하면서 드러났다 (2026-09-23).

`M-047 run_interconnection_study` (Phase 4, G2) 의 입력에 `SLD` 가 있다. 그런데 `SLD` 를
산출하는 미션은 `M-058 generate_single_line_diagram` (Phase 5, G3) 으로 **뒤 단계다.**

```
M-047 (Phase 4) --입력 SLD--> M-058 (Phase 5) 이 산출
```

이대로 선행관계를 만들면 순환이 생긴다. 실제로 29건 확정 직후 검사기가 잡았다.

```
선행관계: 순환 M-047 -> M-058 -> M-050 -> M-049 -> M-047
```

M-049 는 `Grid` 로 M-047 에, M-050 은 `Market` 으로 M-049 에, M-058 은 `Power`·`Grid` 로
M-050 에 걸린다. 여기에 M-047→M-058 이 더해지면 닫힌다.

### 성격

계통연계 검토에 단선도가 필요하고 단선도를 그리려면 계통조건이 필요한, 설계에서 흔한
반복(iteration)이다. 실무에서는 예비 단선도로 검토를 시작해 상세 단선도로 확정한다.
문제는 카탈로그가 이 반복을 미션 하나씩으로만 적어 두어 DAG 로 표현되지 않는다는 점이다.

### 현재 처리 — 결정 전 기록

`build_mission_definitions.py` 는 뒤 미션을 가리키는 입력을 선행관계로 만들지 않고
`extensions.dependency_derivation.forward_references` 에 따로 남겼다. 이 상태는 D-029 결정
전의 기록이며, 기준선으로 사용하지 않는다.

### 조치 — 완료 (2026-09-23, D-029)

새 미션 ID를 삽입하면 100개 미션·단계·Gate 정합성을 다시 깨뜨리므로, M-046의 산출물
계약을 `PreconsultationResult·PreliminarySLD`로 확장했다. M-047은 `PreliminarySLD`를
입력으로 받고, M-058은 `DetailedSLD`를 산출하며, M-060은 `DetailedSLD`를 사용한다.

따라서 선행관계는 `M-046 → M-047`로 닫히고 M-047→M-058 전방참조는 사라진다. 예비
SLD는 G2 계통연계 검토용이고, 상세 SLD는 G3 개념설계 패키지용이다. 예비 SLD 수정이
필요하면 M-046/M-047을 해당 개정으로 반복 실행하며, M-058을 M-047의 선행조건으로
만들지 않는다.

---

## F-07 (중대) 정본의 입력·출력 칸이 공통 엔티티 어휘가 아니다

F-08 을 조사하면서 드러났다 (2026-09-23). F-08 은 이 문제의 한 사례다.

미션 선행관계 DAG 의 유일한 근거는 카탈로그의 입력·출력 칸이다. 두 칸이 같은 어휘를
쓰지 않으면 DAG 를 기계로 도출할 수 없다.

```
python tools/audit_catalog_io.py
```

| 항목 | 수 |
|---|---|
| 입력 토큰 | 235 |
| 그중 앞선 산출물과 연결되는 것 | **101 (42%)** |
| 전방참조 (뒤 미션이 산출) | 0 — F-08 해소(D-029) |
| 명명 드리프트 (같은 것을 다른 이름으로) | 4 |
| 미산출 입력 (어느 미션도 산출하지 않음) | 129 |
| 고아 산출 (어느 미션도 쓰지 않음) | 37 (게이트·종단 제외 29) |

### 명명 드리프트 4건

| 미션 | 입력 | 실제 산출 |
|---|---|---|
| M-007 | `MissingFields` | M-006 `MissingFieldList` |
| M-010 | `Assumptions` | M-007 `AssumptionRegister` |
| M-059 | `Hazards` | M-027 `HazardMap` |
| M-088 | `Hazards` | M-027 `HazardMap` |

### 고아 산출이 말해주는 것

`M-085 install_equipment` 는 `AssetRegister` 를 내는데 이를 입력으로 받는 미션이 없다.
바로 다음 `M-086 run_qa_qc` 의 입력은 `Assets·ITP·Inspections` 다. 두 미션은 개념적으로
이어지는데 이름이 달라 사슬이 끊긴다. `M-089` 의 `AcceptanceResults` 와 `M-090` 의 입력
`Tests` 도 같다.

즉 미션 사이의 논리는 있는데 **어휘가 없어서** 연결되지 않는다. 129건의 미산출 입력도
상당수가 같은 이유일 것이나, 외부 데이터(현장·벤더·시장·기상)와 구분하려면 입력 계약이
있어야 한다. 지금은 없다 (F-02).

### 영향

- 도출된 선행관계는 100건·66개 미션이다. 34개 미션은 선행관계가 비어 있다. 실제로
  선행관계가 없어서가 아니라 어휘가 끊겨서다.
- `input_contract.schema_ref` / `output_contract.schema_ref` 를 채울 수 없다. 참조할
  엔티티 정의가 없다.
- F-02 의 나머지 작업(완료기준·차단조건)도 입력이 무엇인지 확정되지 않으면 쓸 수 없다.

### 필요한 결정

엔티티 사전을 만들어야 한다. 산출 101종을 정본 엔티티로 등록하고, 입력 칸을 그 이름으로
통일한 뒤, 남는 입력을 외부 데이터로 분류해 출처를 붙인다. 이것이 F-02 의 실질적
선행조건이다. 기계가 후보는 낼 수 있으나 어느 입력이 어느 엔티티인지는 자격자가 정한다.
